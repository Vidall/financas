{
  "name": "financas-web",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "start": "pnpm -r start",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev"
  },
  "devDependencies": {
    "@types/node": "^20.19.41"
  }
}
